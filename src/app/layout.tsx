import type { Metadata } from "next";
import { Inter, Montserrat } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";
import ContactBubble from "@/components/ui/ContactBubble";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const montserrat = Montserrat({
  variable: "--font-montserrat",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Solid Foundation Geotechnical | Premium Sydney Engineers",
  description: "Boutique engineering consultancy providing expert Residential and Construction phase support in Sydney. Specialising in inner-west and CBD regions.",
  keywords: ["Soil Report Sydney", "Geotechnical Investigation Inner West", "Pile Inspection Sydney"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${inter.variable} ${montserrat.variable} antialiased min-h-screen flex flex-col`}
      >
        <Navbar />
        <main className="flex-grow">
          {children}
        </main>
        <Footer />
        <ContactBubble />
      </body>
    </html>
  );
}
